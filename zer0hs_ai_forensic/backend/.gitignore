# Secrets — never commit real keys
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# Case data saved by the feedback loop (contains analysis metadata)
feedback/*.json

# Persistent confirmed-indicator memory (local runtime state, not source)
confirmed_indicators.json

# Test artifacts
.pytest_cache/
htmlcov/
.coverage
