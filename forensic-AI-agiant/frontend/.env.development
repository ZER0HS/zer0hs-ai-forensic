VITE_API_URL=http://localhost:8000
VITE_LLM_PROVIDER=ollama
VITE_OLLAMA_MODEL=qwen2.5:14b
# Must match API_KEY in backend/.env — leave blank to disable the header
# (only fine while the backend's own API_KEY is also unset, i.e. local dev).
VITE_API_KEY=